spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    url: jdbc:mysql://localhost:3306/SkillSync_DB?createDatabaseIfNotExist=true
    username: root
    password: Uq>)=5JWJaBw
    driver-class-name: com.mysql.cj.jdbc.Driver

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email
            redirect-uri: "http://localhost:8082/login/oauth2/code/google"
            client-name: Google

  mail:
    host: smtp.gmail.com
    port: 587
    username: supeshalau@gmail.com
    password: heuiezkwclhbdvkd
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8082

paypal:
  client-id: 650663071019-vcjmjevti5037ol1j00mvobrr6a01uni.apps.googleusercontent.com
  client-secret: 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
  mode: sandbox #use sandbox for testing

